# Local Services

The template's local platform is expected to include PostgreSQL, Redis,
Keycloak, and Mailpit once orchestration is introduced.

Startup commands, ports, data persistence, and reset behavior should be
documented here when local orchestration is introduced.
